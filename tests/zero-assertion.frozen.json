[
  "tests/account-docs-generated.test.ts :: the profile contract states what the code states claims a migration only where a client manifest proves one",
  "tests/booking-conversation-lifecycle.test.js :: conversation creation follows the booking, not a screen open the customer app already treats 404 as \"not assigned yet\"",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 020-payment-superseded-sessions.sql sets ownership on every object it creates",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 021-backfill-submitted-onboarding-cases.sql sets ownership on every object it creates",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 021-catalog-v2-backfill.sql sets ownership on every object it creates",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 023-catalog-v2-service-families-view.sql sets ownership on every object it creates",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 025-catalog-v2-services-sequence.sql sets ownership on every object it creates",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 026-otp-purpose.sql sets ownership on every object it creates",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 027-booking-lifecycle-timestamps.sql sets ownership on every object it creates",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 028-booking-synthetic-marker.sql sets ownership on every object it creates",
  "tests/catalog-semantic-guards.test.ts :: semantic guard: catalog migrations stay deployment-safe 029-capability-canonical-source.sql sets ownership on every object it creates",
  "tests/cross-platform-convergence.test.ts :: the parity matrix reports honestly reports mixed rather than rounding a half-migrated capability",
  "tests/eligibility-pipeline.test.ts :: EVERY producer asks the conflict question the same way services/adminBookingService.ts builds its occupancy list from the shared declaration",
  "tests/eligibility-pipeline.test.ts :: EVERY producer asks the conflict question the same way services/booking/transitionExecutor.ts builds its occupancy list from the shared declaration",
  "tests/eligibility-pipeline.test.ts :: EVERY producer asks the conflict question the same way services/providerEligibilityEngine.ts builds its occupancy list from the shared declaration",
  "tests/eligibility-pipeline.test.ts :: EVERY producer asks the conflict question the same way services/technicianService.ts builds its occupancy list from the shared declaration",
  "tests/finance-docs-generated.test.ts :: the document states what the code states claims a migration only where a client manifest proves one",
  "tests/home-docs-generated.test.ts :: the contract states what the code states claims a migration only where a client manifest proves one",
  "tests/messaging-docs-generated.test.ts :: the document states what the code states claims a migration only where a client manifest proves one",
  "tests/migration-checksum.test.ts :: every migration on disk hashes deterministically hashing the raw BYTES would differ from hashing the normalised text on this platform",
  "tests/notification-docs-generated.test.ts :: the contract states what the contract states claims a migration only where a client manifest proves one",
  "tests/parity-registry-hazards.test.js :: parity registry — the Angular copies customer web documents whether its index is single- or multi-valued",
  "tests/parity-registry-hazards.test.js :: parity registry — the Angular copies customer web keeps the id group contextual",
  "tests/v1-contract.test.ts :: planned entries are documented, not built each planned entry names either a legacy route it will replace or why nothing exists"
]
