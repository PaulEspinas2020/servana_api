{
  "masterCommand": "SERVANA API MASTER COMMAND (12 TABs, from repo state 9016e7e) — supersedes the 15-TAB Backend Centralization Command Book",
  "currentTab": "TAB 02 — Make migrations the sole schema authority",
  "currentTabIndex": 2,
  "totalTabs": 12,
  "completedTabs": [
    "TAB 01 (bar the concurrency suite)",
    "TAB 03 (bar real-boot tests)",
    "TAB 04 (rules + parity + audit)",
    "TAB 12 (credential + high CVEs)"
  ],
  "currentObjective": "TAB 02 rescoped 2026-08-17. Authoring gap CLOSED by 036. Deletion phase: 148 -> 3 statements, 106 -> 3 objects, startup graph 19 -> 6 deps (ONE required, and it seeds DATA). The only runtime DDL left is chat.repository, which is deferred.",
  "status": "IN PROGRESS - TAB 02 deletion phase 148 -> 3; 98% done, remainder is the deferred chat.repository",
  "supervisorCycleCount": 15,
  "lastHourlyReportAt": null,
  "lastCheckpointAt": "2026-08-18 - TAB 02: 148 -> 3, migration 037 authored",
  "lastLocalCommit": "see git log — do not trust a recorded SHA, two handoffs running recorded a stale one",
  "workingTreeState": "CLEAN — check `git rev-list --count origin/main..HEAD` for the real number",
  "activeBlockers": [
    "No real boot since app.ts startup was rewritten. It was 19 dependencies; TAB 02 has removed 13, so it is 6 - and only ONE is still `required` (admin-permission-seed, which seeds DATA not schema). The payment, identity and BOTH booking required entries are gone. Every removal is unverified against a running server.",
    "Deploy decision: a push to main IS the deploy, and conflicts with the standing local-only rule (0 of 108 endpoints migrated)",
    "Migration 036 is authored, gated and committed but NOT applied. Production lacks four tables its own booking code writes to. This gates deletion of the runtime DDL for THOSE SIX OBJECTS ONLY — the other 3 statements (all in the deferred chat.repository) target objects production already has and are safe to delete now.",
    "P1 migration 037 authored, PROVEN on PGlite, NOT APPLIED. It drops 39 redundant GLOBAL unique constraints that defeat notification idempotency in production today. Destructive, so it needs the same explicit authorisation 030-035 got.",
    "booking-postgres-races needs a disposable PostgreSQL; PGlite cannot substitute (single-connection)",
    "9 moderate CVEs need npm audit fix --force: major bumps of firebase-admin (auth path) and node-cron (every scheduled job)"
  ],
  "openItems": [
    "P0 apply migration 036 to production, then delete the runtime DDL it replaces (separate commit, in that order)",
    "P1 DEFECT notification_key idempotency has NEVER worked. Production carries 39 GLOBAL UNIQUE (notification_key) constraints (provider_notifications _key1.._key37, customer_notifications _key1/_key2). Both migration 015 and the runtime DDL tried to DROP the UNSUFFIXED name, which has never existed. ON CONFLICT (worker_uid, notification_key) does not absorb a violation of a different constraint, so any deterministic non-owner-scoped key fails for every recipient after the first - scheduler.ts daily_active_bookings_${day} is ONE KEY PER DAY. Migration 037 fixes it by enumerating pg_constraint; PROVEN on PGlite (39 -> 0, owner indexes survive, idempotent). AUTHORED, NOT APPLIED - destructive, needs authorisation.",
    "P1 DEFECT provider_source_attribution had TWO incompatible runtime definitions behind CREATE TABLE IF NOT EXISTS. Production has the providerOnboardingService shape (PK uid), so GET /admin/providers/:uid/attribution and POST /admin/providers/attribution/backfill fail with 42703 today. Needs a decision on the canonical shape plus a migration; admin portal reads one shape, provider-web writes the other.",
    "P1 3 runtime DDL statements left, ALL in chat.repository, which is deferred (see the next item). Everything else is done: 22 services, startup graph 19 -> 6.",
    "P1 technicianService.ensureOnboardingTable is the LAST gated deletion: it creates worker_onboarding, which migration 036 claims and production lacks. Six sibling bootstraps in that same file were removed; this one waits for the 036 apply.",
    "P1 chat.repository ensureChatLifecycleSchema DEFERRED: it also runs a DML derivation (UPDATE chat_conversations SET status=CLOSED WHERE is_closed=TRUE), and is_closed has three consumers. Needs the writers traced before the derivation can be dropped. See project_servana_booking_conversation.",
    "P1 ownership unverified on a real engine — PGlite has no role separation; the `fresh` CI job activates but has never been observed running",
    "P1 9 moderate CVEs need major bumps of firebase-admin and node-cron",
    "P2 scripts/** is typechecked by neither tsconfig",
    "P2 two call sites interpolate the ADD COLUMN name from a loop, so no static scan can resolve them; both were read and are covered (adminBookingService:170, experienceStore:156)",
    "P3 v1-mount-parity.test.ts is deletable once v1-router migrates to the composed app"
  ],
  "pendingQuestions": [],
  "researchDecisions": [
    "PGlite over pg-mem or a server install — real PostgreSQL source compiled to WASM, so disagreements are meaningful",
    "engine-proven asserted as a SUBSET of model-reported — the engine stops each file at its first error",
    "baseline-version ledger (Flyway/Sqitch pattern) rather than replaying a chain that is spent",
    "pending count read back OUT of the database, not derived from the SQL that wrote it",
    "the baseline counts as a schema AUTHORITY, not just a test fixture — db:verify:embedded restores it and applies pending on top, so what it declares is reproducible without runtime DDL. This is what rescoped TAB 02.",
    "classification checked against the ENGINE (restore baseline into PGlite, ask PostgreSQL) rather than trusting the regex that produced it",
    "two CREATE TABLE IF NOT EXISTS for one object is a race with a silent loser, not idempotence - gated by contestedObjects() in schema-authority; all 7 audited against the baseline, 0 unsatisfiable beyond the provider_source_attribution P1"
  ],
  "contextPressure": "moderate",
  "gates": {
    "npm run verify": "PASS - 265 suites, 5769 tests (exit code checked directly, not through a pipe)",
    "npm run db:verify:embedded": "PASS — 121 restored + 7 applied = 132 tables",
    "npm run schema:authority": "PASS - UNMANAGED 0, columns 0, contested 1, 1 invisible index, 1 interpolated column read by hand",
    "npm run ddl:inventory": "3 unmanaged / 3 objects - exits 1 BY DESIGN. All three are chat.repository.",
    "npm run authz:legacy": "615 legacy routes, 0 v1 loosenings",
    "npm run migrations:baseline:plan": "exits 1 BY DESIGN — 14 DML migrations need human judgement"
  },
  "productionActions": {
    "reads": "one — pg_dump --schema-only over SSH, explicitly authorised",
    "writes": "TWO, explicitly authorised 2026-08-16: created servana.schema_migrations (30 rows) and applied migrations 030-035. Verified: 36 ledger rows, 128 tables, 0 not owned by admin, servana-prod 0 restarts. NOTHING since.",
    "deploys": "none",
    "credentialChanges": "none"
  }
}